@page
@model TP_1.Pages.Brands.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Branding[0].BrandingNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Branding) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrandingNome)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BrandingId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BrandingId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BrandingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
